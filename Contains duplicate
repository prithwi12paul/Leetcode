  def containsDuplicate(self, nums: List[int]) -> bool:
        
        for i in range(len(nums)):
            c=0
            for j in range(len(nums)-1):
                if nums[i]==nums[j+1]:
                    c=c+1
                    if c==1:
                        return True
                    else:
                        return False
